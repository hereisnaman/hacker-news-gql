type Link {
  id: ID! @unique
  description: String!
  url: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  postedBy: User
}

type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
  links: [Link!]!
}
